# .github/workflows/azure-functions-deploy.yml

name: CI/CD – Azure Functions

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Sjekk ut koden
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2. Sett opp Python 3.11
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      # 3. Installer dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 4. Deploy til Azure Functions
      - name: Deploy to Azure Functions
        uses: azure/functions-action@v1
        with:
          app-name: 'dummyjson-to-blob-func'                  # Navnet på Function App i Azure
          publish-profile: ${{ secrets.AZURE_FUNCTIONAPP_PUBLISH_PROFILE }}
          package: '.'                                        # rotmappen til prosjektet

      # 5. (Valgfritt) Verifiser ved å gjøre et HTTP-kall
      #- name: Smoke test
      #  run: curl --fail https://dummyjson-to-blob-func.azurewebsites.net/api/run-pipeline
